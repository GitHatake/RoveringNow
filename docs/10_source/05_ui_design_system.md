# 詳細設計書 05 — UIデザインシステム

| 項目 | 内容 |
|---|---|
| 文書名 | UIデザインシステム |
| バージョン | 3.0 |
| 作成日 | 2026-08-17 |
| 最終更新日 | 2026-08-17 |
| ステータス | レビュー中 |
| 対象工程 | 工程1（詳細設計） |
| 上位文書 | [基本設計書](01_basic_design.md) / [アーキテクチャ](02_architecture.md) |

### 改訂履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1.0 | 2026-08-17 | 初版。無彩色（ウォームグレー）＋深緑1色 |
| 2.0 | 2026-08-17 | **配色を RCJ（全国ローバースカウト会議）系統に変更。** 白ベースで、ROVERPORT のブランド色 `#04384c` を見出しに、同じ色相から起こした `#0f6b8a` を操作色に用いる。ニュートラルをウォームグレーからクールグレーへ、コレクションの箔を金から RCJ のトープ `#9b8d77` へ変更。あわせて連絡を白いカードとして分離した |
| 3.0 | 2026-08-17 | **テーマを白ベースに固定**し、端末の暗色設定に追随しないようにした（決定 T-79・決定 T-36 の撤回）。あわせて、簡潔さを保ちながら質感を上げるため、淡い影の二枚重ね・塗り面のグラデーション・グループのモノグラム・ヘッダーとタブバーの半透明化を導入した（決定 T-80〜T-83） |

### 本書の位置づけ

画面ごとの仕様ではなく、**すべての画面が共有する語彙**を定める。色・文字・間隔・部品・動きの決まりごとを1か所に集約し、画面詳細設計（`06_screen_spec.md`）はここで定義した部品を組み合わせて記述する。

デザインの方向性は基本設計フェーズで確定している。

> **ミニマル基調＋コレクションにのみ質感**

---

## 目次

1. [デザイン原則](#1-デザイン原則)
2. [カラー](#2-カラー)
3. [タイポグラフィ](#3-タイポグラフィ)
4. [スペーシングとレイアウト](#4-スペーシングとレイアウト)
5. [形状と奥行き](#5-形状と奥行き)
6. [コンポーネント](#6-コンポーネント)
7. [コレクションの質感](#7-コレクションの質感)
8. [モーション](#8-モーション)
9. [アクセシビリティ](#9-アクセシビリティ)
10. [「開きたくなる」ための設計](#10-開きたくなるための設計)
11. [実装方針](#11-実装方針)
12. [意思決定履歴](#12-意思決定履歴)

---

## 1. デザイン原則

### 1.1 二層構造

このアプリは、性格の異なる2つの領域を持つ。**同じ見た目にしてはならない。**

| 領域 | 該当画面 | 性格 | 表現 |
|---|---|---|---|
| **実用の層** | タイムライン、連絡詳細、グループ、設定、管理 | 読む・判断する・操作する | 無彩色を基調に、余白で構造を示す。装飾を持たない |
| **愛着の層** | コレクション（スタンプ・カード） | 眺める・集める・振り返る | 紙の地と箔押し風の縁。集めたものが「もの」として見える |

**実用の層は読みやすさに徹し、愛着の層にのみ質感を差す。** 全面を質感で覆うと連絡が読みにくくなり、全面をフラットにするとコレクションが「ただのリスト」になる。この使い分け自体が、アプリの人格になる。

### 1.2 5つの原則

| # | 原則 | 意味 |
|---|---|---|
| 1 | **色は語らない、余白が語る** | 情報の階層は、色数ではなく余白と面の差で示す。使う色は無彩色とアクセント1色だけ |
| 2 | **1画面に主行動は1つ** | 主要なボタンは画面に1つだけ。複数あるなら、その画面は2つの仕事をしている |
| 3 | **既定値で完成している** | 設定しなくても使える。設定は「変えたい人が変える」ためにある |
| 4 | **状態を隠さない** | 読み込み中・空・失敗のそれぞれに固有の表示を持つ。何も出さない時間を作らない |
| 5 | **人を追い詰めない** | 未達成・未設定・未所属を、赤や警告として示さない（基本設計書 プロダクト原則7） |

原則5は、基本設計で「所属未確認」表示を廃止した判断（決定38）と同じ考え方である。**状態の可視化が本人の不利益に働いてはならない。**

---

## 2. カラー

### 2.1 考え方

配色は **RCJ（全国ローバースカウト会議）の系統**に合わせる。本アプリは RCJ が運営する ROVERPORT と連携し（Phase 2）、同じ利用者層に向けたものであるため、色の系統を揃えることで「同じ運動のもの」だと伝わる。

実際に用いられている色は次のとおり。

| 出所 | 色 | 用途 |
|---|---|---|
| ROVERPORT | `#04384c` | 見出し色（テーマ変数 `--color_htag`）。ブランドの中心 |
| ROVERPORT | `#fdfdfd` | 背景（ほぼ白） |
| RCJ | `#9b8d77` | 温かみのあるトープ |
| RCJ | `#e2e5e8` / `#b8bcc0` | クールグレー |

これを踏まえ、次の3点で構成する。

1. **白ベース。** 面は白、地はごく淡く色づいた白。余白と面の差だけで階層を示す
2. **見出しはブランド色そのもの（`#04384c`）。** ROVERPORT が見出しに使っているのと同じ扱いにする
3. **操作色は、同じ色相から起こした明るい青（`#0f6b8a`）。** 深い `#04384c` は権威的だが硬い。同系のまま明度と彩度を上げることで、家族性を保ったまま軽さが出る

**アクセントは1色に絞る。** `#0f6b8a` が現れる場所は「押せる」「今ここ」「達成した」のいずれかに限られる。

無彩色には**わずかに冷たい灰（クールグレー）**を用いる。RCJ の `#e2e5e8` / `#b8bcc0` の系統であり、白ベースの清潔さと相性がよい。

### 2.2 ライトテーマ

**ニュートラル（クールグレー）**

| トークン | 値 | 用途 |
|---|---|---|
| `--c-bg` | `#f7f9fa` | 画面の地。ごく淡く色づいた白 |
| `--c-surface` | `#ffffff` | カード・シート・ヘッダーの面 |
| `--c-surface-sunken` | `#eef2f5` | 入力欄、控えめな区画 |
| `--c-border` | `#dde5e9` | 境界線 |
| `--c-border-strong` | `#c2ced5` | 強調した境界線 |
| `--c-text-subtle` | `#5c6b74` | 補助的な文字（日時、件数） |
| `--c-text-muted` | `#41535d` | 二次的な文字 |
| `--c-text` | `#1f2d35` | 本文 |
| `--c-text-strong` | **`#04384c`** | 見出し。**RCJ／ROVERPORT のブランド色** |

**アクセント（ブランド色から起こした青）**

| トークン | 値 | 用途 |
|---|---|---|
| `--c-accent-bg` | `#e8f4f8` | 選択中の行、控えめな強調の背景 |
| `--c-accent-border` | `#a5d0e0` | 強調した枠線 |
| `--c-accent` | `#0f6b8a` | **主要ボタン、選択中のタブ、リンク** |
| `--c-accent-hover` | `#0a5972` | 押下・ホバー |
| `--c-accent-text` | `#0a4f66` | 淡い背景の上に置く文字 |
| `--c-on-accent` | `#ffffff` | **アクセント面の上に置く文字**（第2.3節参照） |

**セマンティック**

| トークン | 値 | 用途 |
|---|---|---|
| `--c-danger` | `#b3261e` | 破壊的操作（脱退・削除・ブロック） |
| `--c-danger-bg` | `#fdecea` | その背景 |
| `--c-warning` | `#8a5a00` | 注意（有効期限が近い等） |
| `--c-info` | `#0f6b8a` | 情報。**アクセント色を流用する**（色を増やさない） |

**コレクション（愛着の層）**

| トークン | 値 | 用途 |
|---|---|---|
| `--c-paper` | `#f8f7f4` | 紙の地 |
| `--c-foil` | **`#9b8d77`** | 箔押し風の縁。**RCJ のトープ** |
| `--c-foil-silver` | `#b8bcc0` | 銀の縁。RCJ のクールグレー |

### 2.3 テーマは固定する

**端末の暗色設定には追随しない。** 白ベースの1組だけを用いる（決定 T-79）。

| 理由 | 内容 |
|---|---|
| ブランドの一貫性 | RCJ 系統の白ベースは、誰の端末でも同じ見え方であってほしい。設定によって印象が変わると、「同じ運動のもの」という手がかりが弱まる |
| 検証の単純化 | 色の組み合わせが1組になるため、追加した色をすべて測定しきれる。2組あると片方の検証漏れが起きる（実際に v2.0 でダーク側の不備が生じた） |
| 屋外での利用 | 活動の現場は明るい環境が多く、白ベースのほうが判読しやすい |

**`color-scheme: light` を宣言する。** これがないと、端末が暗色設定のときにフォーム部品・日付ピッカー・スクロールバーだけがブラウザ既定の暗色で描かれ、画面の中で浮く。あわせて `themeColor` も単一値にする。

> **失った点も記しておく。** 暗い場所での利用や、暗色を好む利用者の好みには応えられない。将来ダークテーマを設けるなら、本節の理由を見直したうえで、両テーマの全組み合わせを測定してから導入すること。

### 2.4 色の使用規則

| 規則 | 内容 |
|---|---|
| アクセントを装飾に使わない | 「押せる」「今ここ」「達成した」にのみ用いる。飾りで色を置かない |
| 赤は取り消せない操作にのみ | 脱退・削除・ブロックなど、影響が戻せない操作に限る。エラー表示には用いるが、**未達成や未設定には用いない**（原則5） |
| 色だけで意味を伝えない | 認証バッジ、状態、エラーはいずれも形または文字を伴う（第9章） |
| グループごとの色分けをしない | タイムラインが色の見本市になる。グループの区別は名前とバッジで行う |

---

## 3. タイポグラフィ

### 3.1 書体

日本語主体のため、端末が持つ書体を用いる。Webフォントは読み込まない。**通知が生命線のアプリで、書体の読み込み待ちが初期表示を遅らせる事態を避ける。**

```
font-family:
  -apple-system, BlinkMacSystemFont,
  "Hiragino Sans", "Noto Sans JP", "Yu Gothic UI", "Meiryo",
  system-ui, sans-serif;
```

数値（リアクション数、メンバー数、スタンプ数）には `font-variant-numeric: tabular-nums` を指定する。数が増減しても桁がずれない。

### 3.2 スケール

| トークン | サイズ / 行間 | 用途 |
|---|---|---|
| `--fs-caption` | 12px / 1.6 | 日時、補助ラベル |
| `--fs-small` | 13px / 1.7 | メタ情報、注記 |
| `--fs-body` | 15px / **1.8** | **本文（連絡・コメント）** |
| `--fs-subtitle` | 17px / 1.6 | 一覧項目の主題、グループ名 |
| `--fs-title` | 20px / 1.5 | 画面の見出し |
| `--fs-display` | 26px / 1.4 | コレクションの見出しなど、限られた場面 |

**本文の行間を1.8と広めに取る。** 日本語は英語より字面が詰まって見えるため、欧文の標準的な行間では読みづらい。連絡を確実に読んでもらうことが本アプリの目的である以上、ここは削らない。

### 3.3 字送りと太さ

| 項目 | 規則 |
|---|---|
| 太さ | 400（本文）と 700（見出し・強調）の2段階のみ。中間の太さは端末により再現されないことがある |
| 字送り | 日本語には指定しない（既定）。英数字の見出しのみ `-0.01em` |
| 行長 | 本文は最大 42文字程度で折り返す。それ以上は目が戻る位置を見失う |

---

## 4. スペーシングとレイアウト

### 4.1 スペーシングスケール

4px を基本単位とする。

| トークン | 値 | 主な用途 |
|---|---|---|
| `--sp-1` | 4px | 密接した要素の間 |
| `--sp-2` | 8px | アイコンと文字の間 |
| `--sp-3` | 12px | 関連する要素の間 |
| `--sp-4` | 16px | **既定の余白。画面の左右余白** |
| `--sp-6` | 24px | 区画の間 |
| `--sp-8` | 32px | 意味のまとまりの間 |
| `--sp-12` | 48px | 画面上下の大きな区切り |

**スケール外の値を使わない。** 「あと2pxだけ」を許すと、余白の一貫性が失われる。

### 4.2 レイアウト

| 項目 | 規則 |
|---|---|
| 画面左右の余白 | `--sp-4`（16px） |
| コンテンツ最大幅 | 640px（PC表示時は中央寄せ） |
| 一覧項目の最小高さ | 56px |
| タップ領域の最小 | **44×44px**。見た目が小さくても当たり判定を確保する |
| セーフエリア | `env(safe-area-inset-*)` を尊重する。タブバーは下部のセーフエリア分だけ内側に置く |

### 4.3 タイムラインの構造

実用の層の代表として、レイアウトの原則を示す。

```
┌────────────────────────────────┐
│                                │ ← --sp-6（区画の間）
│  すみだローバースカウト協議会 ✓  │   --fs-subtitle / 700
│  10月12日の集会について          │   --fs-body / 700
│                                │ ← --sp-3
│  今週土曜、いつもの公民館に9時    │   --fs-body / 400 / 行間1.8
│  集合です。持ち物は…             │
│                                │ ← --sp-4
│  ◯◯◯＋3人が参加            │   アバター重ね + --fs-small
│                                │ ← --sp-3
│  了解 4      参加したい 6        │   --fs-small / --c-text-muted
│                                │
├────────────────────────────────┤ ← --c-border（1px）
│                                │
│  たまがわローバー会 ✓            │
└────────────────────────────────┘
```

**連絡は白いカードとして地から分離する**（決定 T-77）。1件ずつの範囲が一目でわかる。影は落とさず、背景色と1pxの境界線だけで面を分ける。

---

## 5. 形状と奥行き

### 5.1 角丸

| トークン | 値 | 用途 |
|---|---|---|
| `--r-sm` | 6px | 入力欄、小さなバッジ |
| `--r-md` | 10px | ボタン、カード |
| `--r-lg` | 16px | シート、モーダル |
| `--r-full` | 9999px | ピル、アバター |

### 5.2 奥行き

**淡い影を2枚重ねる**（決定 T-80）。近接（1〜2px）と遠方（4〜28px）を組み合わせることで、単一の濃い影より落ち着いた質感になる。

| トークン | 用途 |
|---|---|
| `--shadow-xs` | 一覧の行、副次的なボタン |
| `--shadow-sm` | 連絡カード、区画 |
| `--shadow-md` | プロフィールカード、シート |
| `--shadow-accent` | 主要ボタン、スキャンの円（アクセント色の影） |

**それでも影は「面の分離」の補助にとどめる。** 主たる手段は背景色と境界線であり、影で階層を作らない。

影を使うのは次の3か所のみ。

| 対象 | 影 |
|---|---|
| ボトムシート・モーダル | `0 -2px 16px rgb(4 56 76 / 10%)` |
| タブバー（スクロール時） | `0 -1px 0 var(--c-border)`（線に近い扱い） |
| **コレクションのスタンプ・カード** | 第7章で別途定義 |

---

## 6. コンポーネント

### 6.1 ボタン

| 種別 | 見た目 | 用途 |
|---|---|---|
| Primary | アクセントの塗り＋ `--c-on-accent` の文字 | 画面の主行動。**1画面に1つ** |
| Secondary | 境界線＋アクセントの文字 | 副次的な行動 |
| Ghost | 背景なし＋`--c-text-muted` | 取り消し、一覧内の軽い操作 |
| Danger | 境界線＋`--c-danger` の文字 | 脱退・削除・ブロック |

| 項目 | 値 |
|---|---|
| 高さ | 44px（標準）／ 36px（小） |
| 左右余白 | `--sp-4` |
| 角丸 | `--r-md` |
| 文字 | `--fs-body` / 700 |
| 無効時 | 不透明度 0.4、操作を受け付けない |
| 処理中 | 文字を保ったまま左にスピナー。**幅を変えない**（押した位置がずれない） |

**Danger を塗りにしない。** 赤い塗りのボタンは押す前から威圧感を与える。境界線と文字色で十分に区別でき、かつ確認ダイアログで最終的に止められる。

### 6.2 認証バッジ

グループが認証済みであることを示す。**色以外の手がかりを必ず持たせる**（基本設計書 第13.4節）。

```
すみだローバースカウト協議会 ✓
                            ↑ アクセント色の丸にチェック / 14×14px
```

| 項目 | 規則 |
|---|---|
| 形 | 円形の地にチェック |
| 色 | `--c-accent` |
| 配置 | グループ名の直後、`--sp-1` の間隔 |
| 代替テキスト | 「認証済みの公式組織」 |
| **未認証時** | **何も表示しない。**「未認証」のラベルは出さない（原則5） |

### 6.3 参加意向の表示

決定36の実装。連絡に「参加したい」を付けた人を、同じ連絡を受け取った人に見せる。

```
（●）（●）（●）＋3人が参加
 ↑ アバター24px、-8px 重ね、最大4人
```

| 項目 | 規則 |
|---|---|
| 表示 | アバターを最大4人まで重ね、以降は「＋N人」 |
| 0人のとき | **何も表示しない**（「まだ誰もいません」と書かない） |
| 自分が含まれる場合 | 自分のアバターを先頭に置く |
| タップ | 参加者の一覧を表示 |

**0人のときに空を明示しない。** 「まだ誰も参加していません」という表示は、最初の1人になることをためらわせる。

### 6.4 リアクション

| 項目 | 規則 |
|---|---|
| 種別 | 「了解」「参加したい」の2つ |
| 未反応 | `--c-text-muted` の文字＋件数 |
| 反応済み | `--c-accent-bg` の地＋`--c-accent` の文字 |
| 件数 | 0のときは数字を出さず、ラベルのみ |
| **「了解」の内訳** | **表示しない。**件数のみ（基本設計書 第7.3節） |

### 6.5 タブバー

5タブ。中央のスキャンのみ扱いを変える。

```
┌──────┬──────┬────────┬──────┬──────┐
│ ホーム │コレク │ ╭────╮ │ 通知  │ マイ  │
│      │ ション │ │ ◉ │ │  ●   │ ページ │
│      │      │ ╰────╯ │      │      │
└──────┴──────┴────────┴──────┴──────┘
                  ↑ アクセント色の円 56px
```

| 項目 | 規則 |
|---|---|
| 高さ | 56px ＋ セーフエリア |
| 選択中 | アイコンと文字を `--c-accent` |
| 中央（スキャン） | アクセント色の円（56px）に `--c-on-accent` のアイコン。**常に強調** |
| 未読 | 通知タブに 8px の点。件数は出さない |

**中央を常に強調する理由。** QRはスタンプ獲得・カード交換・グループ参加の共通入口であり（決定18）、活動の現場では数秒で到達する必要がある。選択状態にかかわらず、常に「ここを押せばよい」と分かる形にする。

### 6.6 入力

| 項目 | 規則 |
|---|---|
| 高さ | 44px（1行）／ 最小88px（複数行） |
| 地 | `--c-surface-sunken` |
| 枠 | なし（未フォーカス）／ `--c-accent` 1.5px（フォーカス時） |
| ラベル | 入力欄の上に配置。プレースホルダで代用しない |
| エラー | 枠を `--c-danger`、下に理由を `--fs-small` で示す |
| 文字数制限 | 上限に近づいたときのみ残数を表示する |

**プレースホルダをラベル代わりにしない。** 入力を始めると消えるため、何を書く欄だったか分からなくなる。

### 6.7 グループ名の重複判定

決定21の摩擦を緩和する要のUI（基本設計書 第7.2節 F-02）。

```
グループ名
┌──────────────────────────────┐
│ 夏キャンプ2027               │
└──────────────────────────────┘
  この名前は使われています

  こちらはいかがですか
  ┌────────────────────────┐
  │ 夏キャンプ2027（東京第1地区） │  ← タップで入力
  └────────────────────────┘
```

| 項目 | 規則 |
|---|---|
| 判定 | 入力停止から400ms後に照会する |
| 使用可 | 控えめなチェックのみ。祝わない |
| 使用不可 | **エラーとして赤くしない。** `--c-text-muted` で事実を述べ、すぐ下に代替案を出す |
| 代替案 | 1〜3件。タップで入力欄へ反映。**強制ではない** |

**重複を赤いエラーにしない。** これは利用者の間違いではなく、単に先に使われていたというだけである。作成の手軽さを損なわないため、事実の提示と提案にとどめる。

### 6.8 空状態

各画面に固有の空状態を用意する。

| 画面 | 表示 |
|---|---|
| タイムライン（未所属） | 「まだどのグループにも参加していません」＋グループを探すボタン |
| タイムライン（所属済み・連絡なし） | 「新しい連絡はまだありません」 |
| コレクション（スタンプ） | 空の枠を薄く並べ、「活動に参加するとここに集まります」 |
| コレクション（カード） | 「QRを読み取ると、出会った人のカードが集まります」＋スキャンへの導線 |

**コレクションの空状態では、空の枠を並べて見せる。** 何も置かないより、これから埋まる場所だと分かるほうが集める動機になる。

### 6.9 破壊的操作の確認

脱退・つながり解除・ブロック・削除に用いる。

| 項目 | 規則 |
|---|---|
| 形式 | ボトムシート |
| 見出し | 何が起きるかを一文で。「本当によろしいですか」と問わない |
| 本文 | **取り消せるかどうかを明示する** |
| ボタン | 実行（Danger）／ 取り消し（Ghost）。取り消しを左に置く |

例：

> **このグループから脱退します**
> 今後このグループの連絡は届きません。もう一度参加することはできます。
> 　[ 取り消し ]　[ 脱退する ]

**「もう一度参加できる」ことを書く。** 戻れると分かっていれば、利用者は操作をためらわない。逆に戻れない操作（グループの削除）では、はっきりそう書く。

---

## 7. コレクションの質感

**このアプリで唯一、装飾を許す領域である。**

### 7.1 地

| 項目 | 値 |
|---|---|
| 背景（ライト） | `#f8f7f4`（白に寄せた、ごくわずかに温かい紙色） |
| 背景（ダーク） | `#141f24` |
| テクスチャ | 極小のノイズを重ねる（不透明度 3% 以下）。画像は使わず CSS で生成する |

紙色は無彩色スケールから意図的に外し、クールグレーに対してわずかに温かくする。**この領域だけ「別の場所」だと感じさせる**ためである。白ベースを崩さない範囲にとどめる。

### 7.2 スタンプ

```
   ╔═══════╗
   ║       ║   縁：1.5px のヘアライン（箔押し風）
   ║   ◉   ║   地：テンプレートで指定した色
   ║       ║   影：0 1px 2px rgba(0,0,0,0.10)
   ╚═══════╝
    夏キャンプ
    2027.08.14
```

| 項目 | 規則 |
|---|---|
| 形 | 円形／六角形／盾形からテンプレートで選択 |
| サイズ | 一覧で 88px、詳細で 200px |
| 縁 | `var(--c-foil)`（RCJ のトープ）または `var(--c-foil-silver)`（クールグレー）のヘアライン1.5px |
| 影 | `0 1px 2px rgb(4 56 76 / 12%)` — 紙に貼られている程度 |
| 配置 | 3列グリッド、`--sp-4` の間隔 |
| 未獲得の枠 | 破線の枠のみ。中身は描かない |

### 7.3 プロフィールカード

| 項目 | 規則 |
|---|---|
| 縦横比 | 63:88（トレーディングカードの比率） |
| 角丸 | `--r-md` |
| 構成 | 上部に色帯 → アバター → 表示名 → 所属（表示する場合のみ）→ 自己紹介 |
| 縁 | 内側に 1px の白いハイライト。厚みを感じさせる |
| 影 | `0 2px 6px rgb(4 56 76 / 14%)` |
| 一覧 | 2列グリッド |

### 7.4 やらないこと

| やらないこと | 理由 |
|---|---|
| 3D回転・傾き | 端末によって性能差が大きく、酔いを誘発する場合がある |
| 光沢の走るアニメーション | 一覧に多数並ぶと目が休まらない |
| 効果音・振動 | 活動の現場（屋外・会議中）で使うため |
| レアリティ表示 | スタンプは活動の記録であり、価値の序列をつけない |

**最後の項目は方針として重要である。** スタンプに「レア度」を持ち込むと、参加しやすい活動と参加しにくい活動に優劣が生まれる。すべての活動参加を等しく記録する。

---

## 8. モーション

| 項目 | 値 |
|---|---|
| 標準の長さ | 180ms |
| 短い変化（押下・切替） | 120ms |
| 画面遷移 | 240ms |
| イージング | `cubic-bezier(0.2, 0, 0, 1)`（ease-out 寄り） |

| 規則 | 内容 |
|---|---|
| 位置より不透明度 | 大きく動かさない。淡く現れる程度に留める |
| 一覧の項目を個別に animate しない | 順に現れる演出は、件数が多いと待たされる |
| **`prefers-reduced-motion` を尊重する** | 指定がある場合、遷移以外のアニメーションを無効化する |
| スタンプ獲得時のみ例外 | 獲得の瞬間だけ、スタンプが軽く弾む（400ms）。**このアプリで最も嬉しい瞬間**であり、ここには演出を置く |

---

## 9. アクセシビリティ

| 項目 | 基準 |
|---|---|
| コントラスト比 | 本文 4.5:1 以上、大きな文字 3:1 以上 |
| 色以外の手がかり | 認証バッジは形（チェック）、エラーは文字、状態はラベルを伴う |
| タップ領域 | 44×44px 以上 |
| 文字サイズ | 端末設定に追随する（`rem` を用い、`px` で固定しない） |
| フォーカス | キーボード操作時に `--c-accent` の輪郭を表示する |
| 代替テキスト | アバター・スタンプ画像に必ず付与する |
| 見出し構造 | 各画面で `h1` から順に用いる |

### 9.1 コントラスト比の実測結果

テーマが1組になったため、実際に用いる組み合わせをすべて測定できる（WCAG 2.1 の相対輝度による算出）。

| 前景 | 背景 | 実測 | 判定 |
|---|---|---|---|
| subtle `#5c6b74` | surface `#ffffff` | **5.51:1** | ✅ |
| subtle `#5c6b74` | bg `#f7f9fa` | **5.22:1** | ✅ |
| subtle `#5c6b74` | sunken `#eef2f5` | **4.90:1** | ✅ |
| subtle `#5c6b74` | tint `#f2f6f8` | **5.07:1** | ✅ |
| subtle `#5c6b74` | accent-bg `#e8f4f8` | **4.91:1** | ✅ |
| subtle `#5c6b74` | paper `#f8f7f4` | **5.14:1** | ✅ |
| muted `#41535d` | surface `#ffffff` | **8.01:1** | ✅ |
| muted `#41535d` | bg `#f7f9fa` | **7.59:1** | ✅ |
| muted `#41535d` | sunken `#eef2f5` | **7.12:1** | ✅ |
| muted `#41535d` | tint `#f2f6f8` | **7.37:1** | ✅ |
| muted `#41535d` | accent-bg `#e8f4f8` | **7.15:1** | ✅ |
| muted `#41535d` | paper `#f8f7f4` | **7.48:1** | ✅ |
| text `#1f2d35` | surface `#ffffff` | **14.14:1** | ✅ |
| text `#1f2d35` | bg `#f7f9fa` | **13.39:1** | ✅ |
| text `#1f2d35` | sunken `#eef2f5` | **12.56:1** | ✅ |
| text `#1f2d35` | tint `#f2f6f8` | **13.01:1** | ✅ |
| text `#1f2d35` | accent-bg `#e8f4f8` | **12.61:1** | ✅ |
| text `#1f2d35` | paper `#f8f7f4` | **13.20:1** | ✅ |
| strong `#04384c` | surface `#ffffff` | **12.54:1** | ✅ |
| strong `#04384c` | bg `#f7f9fa` | **11.87:1** | ✅ |
| strong `#04384c` | sunken `#eef2f5` | **11.14:1** | ✅ |
| strong `#04384c` | tint `#f2f6f8` | **11.53:1** | ✅ |
| strong `#04384c` | accent-bg `#e8f4f8` | **11.18:1** | ✅ |
| strong `#04384c` | paper `#f8f7f4` | **11.70:1** | ✅ |
| accent `#0f6b8a` | surface `#ffffff` | **6.02:1** | ✅ |
| accent `#0f6b8a` | bg `#f7f9fa` | **5.70:1** | ✅ |
| accent `#0f6b8a` | sunken `#eef2f5` | **5.34:1** | ✅ |
| accent `#0f6b8a` | tint `#f2f6f8` | **5.53:1** | ✅ |
| accent `#0f6b8a` | accent-bg `#e8f4f8` | **5.37:1** | ✅ |
| accent `#0f6b8a` | paper `#f8f7f4` | **5.62:1** | ✅ |
| accent-text `#0a4f66` | accent-bg `#e8f4f8` | **8.06:1** | ✅ |
| danger `#b3261e` | surface `#ffffff` | **6.54:1** | ✅ |
| danger `#b3261e` | danger-bg `#fdecea` | **5.72:1** | ✅ |
| warning `#8a5a00` | surface `#ffffff` | **5.93:1** | ✅ |
| on-accent `#ffffff` | accent `#0f6b8a` | **6.02:1** | ✅ |
| on-accent `#ffffff` | グラデーション上端 `#18789a` | **5.01:1** | ✅ |
| on-accent `#ffffff` | グラデーション下端 `#0a5972` | **7.82:1** | ✅ |
| foil（装飾） `#9b8d77` | paper `#f8f7f4` | **3.03:1** | ✅ |

`foil` は文字ではなく装飾の縁であるため、3:1 を目安としている。
グラデーション（`--grad-accent`）は上端・下端の両方で白文字が基準を満たす。

**測定によって各版で不備が見つかり、採用前に修正した。**

| 版 | 見つかった不備 | 対処 |
|---|---|---|
| 1.0 | `--c-text-subtle` に割り当てた `#A8A29E` が白地で **2.52:1** | 無彩色の階調を1段ずつ濃くした |
| 2.0 | ダークテーマのアクセント上の白文字が **2.41:1** | `--c-on-accent` を役割として切り出した |
| 3.0 | グラデーションの明るい端の候補 `#2490b4` が **3.67:1** | 上端を `#18789a`（5.01:1）に抑えた |

**新しい組み合わせを追加するときは、必ず測定してから採用する。** 「薄いグレーで控えめに」という判断が、そのまま読めない文字を生む。

---

## 10. 「開きたくなる」ための設計

基本設計のレビューで、「用がないのに開く理由が一つもない」という指摘があった。機能は揃っているが、通知が来たときにしか起動しない状態では、課題1（情報伝達）は解けても課題2（参加意欲）は解けない。

デザインで手当てできる範囲を、次の4点として定める。

| # | 仕掛け | 置き場所 | ねらい |
|---|---|---|---|
| 1 | **参加意向の顔ぶれ** | タイムライン（第6.3節） | 「誰が行くか」が見えることが、開く理由になる |
| 2 | **コレクションの未獲得枠** | コレクション（第7.2節） | 埋まっていない場所が見えると、埋めたくなる |
| 3 | **スタンプ獲得時の演出** | 獲得直後（第8章） | このアプリで最も嬉しい瞬間を、はっきり嬉しくする |
| 4 | **タブの控えめな新着表示** | コレクションタブ | 新しいスタンプ・カードがあるときだけ点を出す |

**これらはデザインで解ける部分にすぎない。** 「開く理由」の本質は、活動そのものが楽しいかどうかにある。アプリができるのは、その楽しさを記録し、次の参加につなげることまでである。過剰なゲーム化（ランキング、連続記録、通知による催促）は行わない。それは参加を義務に変え、課題2をかえって悪化させる。

---

## 11. 実装方針

### 11.1 トークン

**すべての値を CSS カスタムプロパティとして1か所に定義する。** コンポーネントは生の値を書かず、必ずトークンを参照する。

```css
:root {
  --c-accent: #0f6b8a;
  --sp-4: 16px;
  --fs-body: 15px;
  /* … */
}

@media (prefers-color-scheme: dark) {
  :root { --c-accent: #4fb3d4; /* … */ }
}
```

ユーティリティCSS（Tailwind 等）の採用は任意とする。採用する場合も、**トークンを唯一の出所とし、設定ファイルからこれを参照する**。生の値をクラス名に直接書くことは禁じる。

### 11.2 コンポーネントの分類

| 層 | 内容 | 例 |
|---|---|---|
| プリミティブ | トークンを直接まとうもの | Button, Input, Badge, Avatar |
| パターン | プリミティブの組み合わせ | PostCard, GroupRow, StampTile, ConfirmSheet |
| 画面 | パターンの配置 | `06_screen_spec.md` で定義 |

**プリミティブは業務を知らない。** `Button` は「連絡を投稿する」を知らず、色と大きさと状態だけを知る。業務の語彙はパターン層から現れる。

### 11.3 テーマ

**トークンの定義は `:root` の1組だけ**とし、`prefers-color-scheme` による上書きを置かない（決定 T-79）。アプリ内の手動切り替えも設けない。設定項目を増やさない方針（原則3）は維持している。

`:root` には `color-scheme: light` を、メタデータには単一値の `themeColor` を宣言する。どちらか一方が欠けると、端末が暗色設定のときに一部だけ暗く描かれる。

---

## 12. 意思決定履歴

| # | 項目 | 決定 | 日付 | 理由 |
|---|---|---|---|---|
| T-26 | 二層構造 | 実用の層はミニマル、愛着の層（コレクション）にのみ質感を差す | 2026-08-17 | 全面を質感で覆うと連絡が読みにくく、全面をフラットにするとコレクションがただのリストになる |
| ~~T-27~~ | ~~配色~~ | ~~無彩色（ウォームグレー）＋深緑1色~~ → **決定 T-75 により RCJ 系統へ変更** | 2026-08-17 | アクセントを1つに絞る方針自体は維持している |
| T-28 | 書体 | 端末の標準書体のみ。Webフォントを読み込まない | 2026-08-17 | 通知が生命線のアプリで、書体の読み込み待ちが初期表示を遅らせる事態を避ける |
| T-29 | 本文の行間 | 1.8 と広めに取る | 2026-08-17 | 日本語は欧文の標準的な行間では読みづらい。連絡を確実に読んでもらうことが目的である以上、ここは削らない |
| ~~T-30~~ | ~~影の使用~~ | ~~実用の層では影を使わない~~ → **決定 T-80 により改訂**（淡い影を二枚重ねて用いる） | 2026-08-17 | テーマを固定したため、ダークテーマでの再現性という懸念が消えた |
| T-31 | Danger ボタン | 塗りにせず、境界線と文字色で表す | 2026-08-17 | 赤い塗りは押す前から威圧感を与える。確認ダイアログで最終的に止められる |
| T-32 | 未達成の非表示 | 未認証・未所属・参加0人を明示しない | 2026-08-17 | 状態の可視化が本人の不利益に働いてはならない（基本設計書 プロダクト原則7・決定38と同じ考え方） |
| T-33 | 名前の重複表示 | エラーとして赤くせず、事実の提示と代替案の提案にとどめる | 2026-08-17 | 重複は利用者の間違いではない。作成の手軽さ（プロダクト原則2）を損なわない |
| T-34 | レアリティの不採用 | スタンプに価値の序列をつけない | 2026-08-17 | 参加しやすい活動と参加しにくい活動に優劣が生まれる。すべての活動参加を等しく記録する |
| T-35 | 過剰なゲーム化の不採用 | ランキング・連続記録・催促通知を設けない | 2026-08-17 | 参加を義務に変え、課題2（参加意欲の低下）をかえって悪化させる |
| ~~T-36~~ | ~~テーマ切り替え~~ | ~~端末設定に追随する~~ → **決定 T-79 により撤回**（白ベースに固定） | 2026-08-17 | 設定項目を増やさない方針は維持している |
| T-37 | トークンの一元管理 | すべての値を CSS カスタムプロパティで定義し、生の値を書かない | 2026-08-17 | ユーティリティCSSを採用する場合も、出所を1つに保つ |
| **T-75** | **配色の系統** | RCJ（全国ローバースカウト会議）の実際の色に合わせ、白ベースとする。見出しは ROVERPORT のブランド色 `#04384c`、操作色は同じ色相から起こした `#0f6b8a` | 2026-08-17 | 本アプリは ROVERPORT と連携し（Phase 2）、同じ利用者層に向けたものである。色の系統を揃えることで「同じ運動のもの」だと伝わる。深い `#04384c` は権威的だが硬いため、明度と彩度を上げた同系色を操作色に用いて軽さを出す |
| **T-76** | **アクセント面の文字色** | `--c-on-accent` を独立したトークンとし、テーマごとに反転させる | 2026-08-17 | ライトではアクセントが暗く白文字が載るが、ダークでは明るいため白文字が 2.41:1 にしかならない。コンポーネント側に `#fff` を直接書かせない |
| **T-77** | **連絡の面** | 連絡を白いカードとして地から分離する | 2026-08-17 | 地をごく淡く色づけたことで、影を使わずに背景色の差だけで面を分けられるようになった（決定 T-30 の方針のまま、1件ずつが見分けやすくなる） |
| **T-78** | **箔の色** | 金 `#C9A227` をやめ、RCJ のトープ `#9b8d77` を用いる | 2026-08-17 | 金は華美で、白ベースの落ち着いた画面から浮く。RCJ が実際に用いている色のほうが、コレクションという「手元に残るもの」の質感に合う |
| **T-79** | **テーマの固定** | 白ベースの1組に固定し、端末の暗色設定に追随しない。`color-scheme: light` を宣言する | 2026-08-17 | 誰の端末でも同じ見え方にしてブランドの一貫性を保つ。色の組み合わせが1組になり、追加色をすべて測定しきれる。宣言がないとフォーム部品だけが暗色で描かれ浮く |
| **T-80** | **奥行きの与え方** | 淡い影を2枚（近接＋遠方）重ねる。実用の層でも用いる | 2026-08-17 | 単一の濃い影は安く見える。テーマを固定したため、ダークテーマでの再現性という T-30 の懸念が消えた |
| **T-81** | **グループのモノグラム** | 連絡カードにグループ名の頭文字を丸で示す | 2026-08-17 | 一覧を目で追うときの手がかりになり、どのグループからの連絡かが文字を読む前に分かる。色を増やさずに情報の階層を作れる |
| **T-82** | **塗り面のグラデーション** | 主要ボタンとスキャンの円に縦方向のグラデーションを敷く | 2026-08-17 | 平坦な単色より質感が出る。上端・下端の両方で白文字が基準を満たす範囲に収める |
| **T-83** | **ヘッダーとタブバーの半透明化** | 背景を 82〜90% の白にし、ぼかしを重ねる | 2026-08-17 | スクロール中に下の内容がわずかに透け、層の重なりが伝わる。不透明な帯より軽く見える |

### 実装時に検証する事項

| # | 項目 | 判断の基準 |
|---|---|---|
| 1 | 端末の暗色設定 | 暗色設定の端末でも、フォーム部品・日付ピッカー・スクロールバーを含めて明色で描かれること |
| 2 | 文字サイズの追随 | 端末の文字サイズを最大にしても、タブバーと主要ボタンが崩れないこと |
| 3 | 紙のテクスチャ | CSS で生成したノイズが、低性能な端末で描画負荷にならないこと |
| 4 | 実機での色の見え方 | 屋外の明るい環境で、アクセント色が判別できること（活動の現場で使うため） |

なお、コントラスト比は本書作成時に**測定済み**であり（第9.1節）、実装時の検証項目からは外している。
