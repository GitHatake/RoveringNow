# RoveringNow — 開発の約束ごと

ローバースカウト向けWEBアプリ。**設計書が開発を主導します。**

## まず読むもの

| 文書 | 内容 |
|---|---|
| `docs/10_source/01_basic_design.md` | 基本設計書。**何を作るか**の唯一の正 |
| `docs/10_source/02_architecture.md` | 技術選定と堅牢性の設計方針 |
| `docs/10_source/03_db_schema.md` | データベース物理設計 |
| `docs/10_source/05_ui_design_system.md` | 色・文字・間隔・部品の定義 |
| `docs/00_read_me/10_implementation_plan.md` | 工程と進捗 |

意思決定は番号で追えます。基本設計は `決定1`〜`決定45`、詳細設計は `T-01`〜`T-74`。
コードのコメントからこれらを参照しています。

## 守ること

### 設計書を先に直す

実装の都合で仕様を変える必要が出たら、**コードより先に設計書を改訂**し、
意思決定履歴に日付と理由を残してください。黙って設計から逸れないこと。

### 認可は1か所だけ

すべてのサーバ処理は `src/domain/authorization.ts` の `can()` を通ります。
個別の画面や Server Action に権限判定を書かないでください。
権限マトリクスに行を足したら `ACTIONS` と `Permission` の両方を更新します
（分岐を書き忘れると型エラーになります）。

`tests/authorization.test.ts` が 175 通りを総当たりで検証しています。

### 守れるものはデータベースに守らせる

一意性・整合性・状態の妥当性は、アプリではなく制約として表現します。
1人開発ではレビューによる品質担保に限界があるためです。

例：`connections` の `CHECK (user_a_id < user_b_id)`、
`users` の「退会状態なら個人特定情報がすべて NULL」。

### サーバ専用モジュールに `import 'server-only'`

データベースに触れるモジュールには必ず付けてください。
クライアント境界に巻き込まれると PGlite がバンドルされ、実行時に壊れます
（実際に起きました）。

### 値はトークンから取る

色・間隔・文字サイズは `src/app/globals.css` の CSS カスタムプロパティを参照します。
生の値を書かないでください。**新しい色の組み合わせを使うときは、
コントラスト比 4.5:1 を満たすか測ってから**採用します。

### 効果（useEffect）で状態を書き戻さない

派生値は描画時に計算します。ESLint がこれを検出します。規則を無効化せず、
設計を直してください。

## 検証

```bash
npm run verify        # lint → 型検査 → テスト（270件）
```

テストは PGlite で実物の PostgreSQL に対して実行します。外部サービスは不要です。
**完了を宣言する前に必ず実行し、結果を示してください。**

## 動作確認

```bash
npm run db:seed && npm run dev
```

`.env.local` に `AUTH_MODE=dev` を書くと認証を省いて画面を確認できます。

見た目を変えたときは、**必ず画像で確認**してください。

```bash
npm run screenshot
```

主要9画面を `.screenshots/` に出力し、ブラウザが実際に計算した色も表示します。
「CSS は正しいのに画面が変わらない」を推測で片付けないための道具です。

詳しくは `README.md` を参照。
